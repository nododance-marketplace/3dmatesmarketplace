# ── NextAuth ─────────────────────────────────────────────────
NEXTAUTH_URL="http://localhost:3002"
NEXTAUTH_SECRET="generate-a-secret-with-openssl-rand-base64-32"

# ── Database ─────────────────────────────────────────────────
# Local dev uses SQLite (file:./dev.db) — no DATABASE_URL needed.
# For production Postgres, switch prisma/schema.prisma back to
# provider = "postgresql" and uncomment this:
# DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"

# ── Email Provider (NextAuth magic links) ────────────────────
# OPTIONAL for local dev. If omitted, the app auto-creates an
# Ethereal test account and prints the preview URL in terminal.
# EMAIL_SERVER_HOST="smtp.gmail.com"
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER="your-email@gmail.com"
# EMAIL_SERVER_PASSWORD="your-app-password"
EMAIL_FROM="3DMates <noreply@3dmates.com>"

# ── Google OAuth (optional — production only) ────────────────
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""

# ── Cloudinary ───────────────────────────────────────────────
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# ── Admin ────────────────────────────────────────────────────
ADMIN_EMAILS="admin@example.com"
